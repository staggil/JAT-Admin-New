function dateInputProc() {
    //var whichCode = event.data
    ////check if not backspace
    //if (whichCode != null) {
    //    var value = this.value
    //    //3 type of slash add->extra full,extra 1st,extra 2nd
    //    if (value.length == 8 && value.indexOf('/') == -1) {
    //        var txt = this.value
    //        var tmp = txt.substr(0, 4) + "/" + txt.substr(4, 2) + "/" + txt.substr(6, 2)
    //        this.value = tmp
    //    }
    //    else if (value.length == 7 && value.indexOf('/', 6) == -1) {
    //        var txt = this.value
    //        var tmp = txt.substr(0, 7) + "/"
    //        this.value = tmp
    //    }
    //    else if (value.length == 4 && value.indexOf('/') == -1) {
    //        var txt = this.value
    //        var tmp = txt + "/"
    //        this.value = tmp
    //    }
    //}
}